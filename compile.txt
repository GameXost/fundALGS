# 1. Перейди в проект
cd /mnt/d/MAI/3semestr/algosi

# 2. Удали старый build (в нем конфликтующие пути)
rm -rf build

# 3. Создай папку build заново
mkdir build

# 4. Запусти CMake конфигурацию в WSL
cmake -S . -B build -G "Unix Makefiles"

# 5. Собери проект
cmake --build build

cmake --build build --clean-first
